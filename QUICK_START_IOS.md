# OnlyMusic iOS - Quick Start Guide

## Самый простой способ (без сборки)

### Вариант 1: TestFlight (рекомендуется для тестирования)
1. Зарегистрируйтесь в [Apple Developer Program](https://developer.apple.com) ($99/год)
2. Загрузите приложение через Xcode
3. Используйте TestFlight для распространения beta-версии

### Вариант 2: Прямая установка (бесплатно, но срок 7 дней)
Можно установить без платного аккаунта, но приложение перестанет работать через 7 дней.

## Полная сборка (для разработчиков)

### Требования
- **macOS** с Xcode
- **Минимум 10 GB** свободного места
- **1-2 часа** времени на первую сборку
- Apple Developer Account (бесплатный или платный)

### Шаг 1: Автоматическая сборка

```bash
chmod +x BUILD_IOS.sh
./BUILD_IOS.sh
```

Скрипт автоматически:
- ✅ Создаст виртуальное окружение Python
- ✅ Установит все зависимости
- ✅ Соберёт iOS toolchain
- ✅ Создаст Xcode проект

### Шаг 2: Открыть в Xcode

```bash
open ~/Desktop/OnlyMusic-iOS/OnlyMusic.xcodeproj
```

### Шаг 3: Настроить подписывание

В Xcode:
1. Выберите проект **OnlyMusic** слева
2. Перейдите на вкладку **Signing & Capabilities**
3. Выберите ваш **Team** (Apple ID)
4. Xcode автоматически создаст сертификат

### Шаг 4: Подключить iPhone

1. Подключите iPhone через USB
2. На iPhone: **Настройки → Основные → VPN и управление устройством** → доверьтесь разработчику
3. В Xcode выберите ваш iPhone как целевое устройство

### Шаг 5: Запуск

Нажмите **▶ Run** (⌘R) в Xcode

## Если что-то пошло не так

### Ошибка: "Failed to build python3"

```bash
# Очистите и пересоберите
source ~/.onlymusic-build-env/bin/activate
toolchain clean all
toolchain build python3
```

### Ошибка: "Code signing failed"

1. Откройте **Xcode → Preferences → Accounts**
2. Добавьте ваш Apple ID
3. Нажмите **Download Manual Profiles**
4. Попробуйте снова

### Ошибка: "Could not launch OnlyMusic"

1. На iPhone: **Настройки → Основные → VPN и управление устройством**
2. Найдите ваш профиль разработчика
3. Нажмите **Доверять**

### Приложение вылетает сразу после запуска

Проверьте логи в Xcode:
- **View → Debug Area → Activate Console**
- Посмотрите на ошибки Python

## Альтернатива: Упрощённая сборка с Pythonista

Если полная сборка не работает, можно использовать **Pythonista** (приложение из App Store):

1. Купите Pythonista 3 ($9.99) в App Store
2. Создайте упрощённую версию с UI toolkit
3. Запускайте прямо из Pythonista

## Минимальная версия (без UI)

Если нужен минимальный работающий плеер:

```python
# mini_player.py
import yt_dlp
import subprocess

def play(query):
    ydl_opts = {'format': 'bestaudio', 'quiet': True}
    with yt_dlp.YoutubeDL(ydl_opts) as ydl:
        info = ydl.extract_info(f"ytsearch:{query}", download=False)
        url = info['entries'][0]['url']
        subprocess.call(['mpv', url])

play("lofi music")
```

## Поддержка

Если возникли проблемы:
1. Проверьте, что Xcode обновлён до последней версии
2. Убедитесь, что у вас установлены Command Line Tools: `xcode-select --install`
3. Попробуйте пересоздать виртуальное окружение: `rm -rf ~/.onlymusic-build-env`

## Полезные команды

```bash
# Активировать окружение для работы с toolchain
source ~/.onlymusic-build-env/bin/activate

# Посмотреть доступные рецепты
toolchain recipes

# Пересобрать конкретный пакет
toolchain clean kivy
toolchain build kivy

# Создать новый проект (если удалили старый)
toolchain create OnlyMusic ~/Desktop/OnlyMusic-iOS
```

## Что делать после успешной установки

1. **Первый запуск**: При первом запуске приложение попросит разрешения на доступ к сети
2. **Поиск**: Введите запрос и нажмите Search
3. **Воспроизведение**: Тапните на трек для воспроизведения
4. **Фоновое воспроизведение**: Музыка продолжит играть при блокировке экрана

Наслаждайтесь! 🎵📱
