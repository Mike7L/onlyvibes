# 🎵 YouTube Music Streamer - Примеры использования

## Быстрый старт

### 1. Интерактивный режим (рекомендуется для начинающих)
```bash
python3 streamer.py
```

### 2. Поиск музыки
```bash
# Поиск lofi музыки
python3 streamer.py -s "lofi hip hop beats"

# Поиск джаза
python3 streamer.py -s "smooth jazz"

# Поиск с большим количеством результатов
python3 streamer.py -s "classical music" -n 20
```

### 3. Прямые ссылки
```bash
# Воспроизведение конкретного видео (только аудио)
python3 streamer.py -u "https://www.youtube.com/watch?v=jfKfPfyJRdk"

# Воспроизведение с видео
python3 streamer.py -u "https://www.youtube.com/watch?v=jfKfPfyJRdk" --video

# Воспроизведение плейлиста
python3 streamer.py -u "https://www.youtube.com/playlist?list=PLwR9W_2XAg"
```

## Популярные запросы

### Фоновая музыка для работы
```bash
python3 streamer.py -s "lofi study beats"
python3 streamer.py -s "ambient music for work"
python3 streamer.py -s "focus music"
```

### Релаксация
```bash
python3 streamer.py -s "meditation music"
python3 streamer.py -s "nature sounds"
python3 streamer.py -s "rain sounds"
```

### Жанры
```bash
python3 streamer.py -s "jazz music"
python3 streamer.py -s "classical piano"
python3 streamer.py -s "rock classics"
python3 streamer.py -s "electronic music"
```

## Управление во время воспроизведения

| Клавиша | Действие |
|---------|----------|
| `Space` | Пауза/Воспроизведение |
| `←` / `→` | Перемотка ±5 секунд |
| `Shift+←` / `Shift+→` | Перемотка ±60 секунд |
| `↑` / `↓` | Громкость ±2% |
| `9` / `0` | Громкость -5% / +5% |
| `m` | Отключить/включить звук |
| `f` | Полноэкранный режим (для видео) |
| `q` | Выход |
| `[` / `]` | Скорость воспроизведения |

## Продвинутое использование

### Создание алиаса для быстрого доступа
Добавьте в `~/.zshrc` или `~/.bashrc`:
```bash
alias yt-music='python3 /Users/micha/Dropbox/Projects/_ME/youtube-music-streamer/streamer.py'
```

Теперь можно использовать:
```bash
yt-music -s "your favorite music"
```

### Использование с tmux/screen
```bash
# Запуск в фоновой сессии
tmux new -s music
python3 streamer.py -s "lofi"
# Нажмите Ctrl+B, затем D для отсоединения
```

## Советы и трюки

1. **Качество звука**: По умолчанию используется лучшее доступное качество аудио
2. **Экономия трафика**: Используйте режим только аудио (по умолчанию)
3. **Плейлисты**: Можно воспроизводить целые плейлисты, передав URL плейлиста
4. **Поиск**: Используйте кавычки для точного поиска: `-s "exact phrase"`

## Устранение неполадок

### Проблема: "yt-dlp not found"
```bash
brew install yt-dlp
```

### Проблема: "mpv not found"
```bash
brew install mpv
```

### Проблема: Медленная загрузка
- Проверьте интернет-соединение
- Попробуйте другое видео
- YouTube может временно ограничивать доступ

### Проблема: Нет звука
- Проверьте системную громкость
- Нажмите `m` в mpv для включения звука
- Проверьте, что аудиовыход правильно настроен

## Интеграция с другими инструментами

### Использование с fzf для интерактивного поиска
```bash
# Установите fzf: brew install fzf
python3 streamer.py -s "music" -n 50 | fzf
```

### Сохранение истории воспроизведения
```bash
echo "$(date): $1" >> ~/music_history.log
python3 streamer.py -u "$1"
```

## Ограничения

- Требуется активное интернет-соединение
- Зависит от доступности YouTube
- Качество зависит от исходного видео
- Некоторые видео могут быть недоступны в вашем регионе
